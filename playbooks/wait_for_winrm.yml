---
- name: Wait for WinRM to become available
  hosts: all
  gather_facts: false

  tasks:

    - name: Poll WinRM until connection succeeds
      ansible.windows.win_ping:
      register: winrm_test
      until: winrm_test is success
      retries: 60
      delay: 10
      ignore_unreachable: true
